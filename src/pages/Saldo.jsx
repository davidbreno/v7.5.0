import { AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid } from 'recharts'
import Sidebar from '../components/Sidebar'
import { load } from '../data/store'
export default function Saldo(){ const data=load(); const serie=(()=>{ let acc=0; return data.financas.transacoes.map(t=>{acc+=t.tipo==='entrada'?t.valor:-t.valor; return {data:t.data, saldo:acc}}) })(); return (<div className="layout"><Sidebar /><div><main className="wrap"><div className="widget"><div className="h-72"><ResponsiveContainer width="100%" height="100%"><AreaChart data={serie}><defs><linearGradient id="g1" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="var(--brand2)" stopOpacity={0.8}/><stop offset="95%" stopColor="var(--brand2)" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="rgba(255,255,255,.08)" /><XAxis dataKey="data" /><YAxis /><Tooltip /><Area type="monotone" dataKey="saldo" stroke="var(--brand2)" fillOpacity={1} fill="url(#g1)" /></AreaChart></ResponsiveContainer></div></div></main></div></div>) }