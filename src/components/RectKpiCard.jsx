import { ResponsiveContainer, LineChart, Line, Tooltip } from 'recharts'
export default function RectKpiCard({ title, value, suffix='', percent, data=[], subtitle, alert }){ return (<div className="thin-outline rounded-2xl p-4 widget-click" style={{background:'linear-gradient(180deg,rgba(255,255,255,.02),rgba(255,255,255,.01))',position:'relative'}}>{alert&&<span style={{position:'absolute',top:10,right:10}} className="px-2 py-1 text-xs rounded bg-red-500/80">Atenção</span>}<div className="flex items-center justify-between"><div><div className="text-sm opacity-80">{title}</div><div className="text-3xl font-extrabold mt-1">{value}{suffix&&<span className="text-base opacity-80 ml-1">{suffix}</span>}</div>{typeof percent==='number'&&<div className="text-xs opacity-80 mt-1">{percent}%</div>}{subtitle&&<div className="text-xs opacity-70 mt-1">{subtitle}</div>}</div><div className="w-40 h-16"><ResponsiveContainer width="100%" height="100%"><LineChart data={data}><defs><linearGradient id="rk" x1="0" y1="0" x2="1" y="0"><stop offset="0%" stopColor="var(--brand1)"/><stop offset="60%" stopColor="var(--brand2)"/><stop offset="100%" stopColor="var(--brand3)"/></linearGradient></defs><Tooltip /><Line type="monotone" dataKey="y" stroke="url(#rk)" strokeWidth={3} dot={false} /></LineChart></ResponsiveContainer></div></div></div>) }