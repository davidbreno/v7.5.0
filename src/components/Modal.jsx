import { useEffect } from 'react'
export default function Modal({ open, title, children, onClose }){ useEffect(()=>{ function esc(e){ if(e.key==='Escape') onClose?.() } document.addEventListener('keydown', esc); return ()=>document.removeEventListener('keydown', esc) },[onClose]); if(!open) return null; return (<div className="fixed inset-0 bg-black/60 flex items-center justify-center z-50"><div className="bg-[color:var(--panel)] rounded-2xl border border-white/10 shadow-panel w-[min(560px,92vw)]"><div className="px-4 py-3 border-b border-white/10 flex items-center justify-between"><div className="font-semibold">{title}</div><button onClick={onClose} className="px-2 py-1 bg-white/10 rounded">Fechar</button></div><div className="p-4">{children}</div></div></div>) }